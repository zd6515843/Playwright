1. Install Node.js
Search node install in browser
https://nodejs.org/en/download/package-manager

echo $PATH

vim ~/.bash_profile
export PATH=$PATH:/usr/local/bin
source ~/.bash_profile
---------------------------------
For windows:
Edit system parameter
  NODE_HOME 

2. Install Visual Studio
Search visual studio code

3. Install Playwright dependencies




To save env file with below steps in sequence
1. ESC button
2. :wq
3. Enter button
